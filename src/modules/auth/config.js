import { Shield } from 'lucide-react'

export default {
  key: 'auth',
  name: 'Authentication',
  description: 'User login, registration, and session management with role-based access control.',
  icon: Shield,
  category: 'core',
}
